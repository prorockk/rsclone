export default function openDoors(currentRoomObjects: any) {
    // currentRoomObjects['door.png'].forEach((element: { _texture: any}) => {
    //     element._texture.orig.width = 10;
    //     console.log(element);
    // });

    // setTimeout(()=>{
    //     currentRoomObjects['door.png'].forEach((element: {_texture: any}) => {
    //         element._texture.orig.width = 50;
    //         console.log(element);
    //     });
    // }, 2000)
    console.log("ok");
}
